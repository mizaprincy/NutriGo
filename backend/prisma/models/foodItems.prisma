model FoodItem {
  food_item_id  Int   @id @default(autoincrement())
  name          String
  calories      Float
  protein       Float
  carbs         Float
  fat           Float
  type          String
}
